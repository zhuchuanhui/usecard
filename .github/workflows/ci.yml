name: CI

on:
  push:
  pull_request:

jobs:
  catalog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: services/catalog/package-lock.json
      - run: npm ci
        working-directory: services/catalog
      - run: npm run typecheck
        working-directory: services/catalog
      - run: npm test
        working-directory: services/catalog

  swift:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: swift test
      - run: ./script/generate_xcode_project.sh
      - name: Verify tagged macOS version metadata
        env:
          APP_VERSION: v9.8.7
        run: |
          ./script/build_macos_app.sh
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' UseCard.app/Contents/Info.plist)" = "9.8.7"
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' UseCard.app/Contents/Info.plist)" = "9.8.7"
      - name: Build iOS app
        run: |
          set -o pipefail
          xcodebuild -project ios/UseCard.xcodeproj -scheme UseCard -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO build
